# 🚀 Quick LINE Setup (5 นาที)

ตอนนี้เชื่อมต่อ LINE ได้บนเว็บแล้ว! ไม่ต้องแก้ไข code เลย

---

## 📋 ขั้นตอนสั้น ๆ

### 1️⃣ สร้าง LINE Official Account (3 นาที)

1. ไปที่ [LINE Developers Console](https://developers.line.biz/console/)
2. Login ด้วย LINE
3. Create Provider (ชื่อบริษัท)
4. Create Channel → เลือก **"Messaging API"**
5. กรอกชื่อ Bot และข้อมูลอื่นๆ

### 2️⃣ เก็บ Credentials (2 นาที)

**ใน LINE Developers Console:**

#### Basic Settings tab:
- **Channel ID** - คัดลอก
- **Channel Secret** - Issue แล้วคัดลอก

#### Messaging API tab:
- **Channel Access Token** - Issue แล้วคัดลอก (long-lived)

---

## 🎨 เชื่อมต่อผ่าน Dashboard

### ใน Anajak Chat:

1. ไปที่ **Settings** → **ช่องทาง** (Channels)
   - หรือไปตรง: `http://localhost:3000/dashboard/settings/channels`

2. คลิก **"เชื่อมต่อ LINE"** ที่การ์ด LINE

3. กรอกข้อมูล:
   ```
   Channel ID: 1234567890
   Channel Secret: abcdef123456789
   Access Token: eyJ... (ยาวมาก)
   ```

4. **ตั้งค่า Auto-reply** (optional):
   - ✅ เปิดใช้งาน Auto-reply
   - แก้ไขข้อความตอบกลับ

5. คลิก **"ทดสอบการเชื่อมต่อ"**
   - ถ้าสำเร็จจะเห็น: ✅ เชื่อมต่อสำเร็จ! Bot name: ...

6. คลิก **"บันทึก"**

---

## ✅ ตั้งค่า Webhook ใน LINE

**สำคัญ!** ต้องตั้งค่านี้ก่อนใช้งานจริง:

### A. Deploy ก่อน (Vercel)

```bash
git add .
git commit -m "Add LINE integration"
git push

# Deploy on Vercel
# Get URL: https://your-app.vercel.app
```

### B. ตั้งค่า Webhook URL

1. กลับไปที่ LINE Developers Console
2. เลือก Channel
3. ไปที่ tab **"Messaging API"**
4. หา **"Webhook settings"**
5. คลิก **Edit**
6. ใส่ URL:
   ```
   https://your-app.vercel.app/api/webhooks/line
   ```
7. คลิก **Update** → คลิก **Verify** (ต้อง Success)
8. เปิด **"Use webhook"** (toggle เป็นสีเขียว)

### C. ปิด Auto-reply ของ LINE

1. ใน tab เดียวกัน หา **"Auto-reply messages"**
2. คลิก **Edit**
3. **ปิด** Auto-reply (เพราะเราใช้ของเราเอง)

---

## 🧪 ทดสอบ

### 1. เพิ่ม Bot เป็นเพื่อน

ใน LINE Developers Console → Messaging API tab:
- Scan **QR code**
- หรือค้นหาด้วย **Bot basic ID**

### 2. ส่งข้อความ

ส่งข้อความไปที่ Bot: "สวัสดีครับ"

### 3. ดูใน Dashboard

1. ไปที่ `/dashboard/inbox`
2. ควรเห็น conversation ใหม่!
3. คลิก Claim
4. ตอบกลับ
5. ลูกค้าได้รับข้อความใน LINE ✅

---

## 💡 Tips

### Development (ใช้ ngrok)

```bash
# Terminal 1: Run app
npm run dev

# Terminal 2: Run ngrok
ngrok http 3000

# ได้ URL: https://xxxx.ngrok-free.app
# ใช้เป็น Webhook URL ชั่วคราว
```

### Production (Vercel)

```bash
# Deploy
vercel --prod

# ใช้ URL: https://your-app.vercel.app
# ตั้งเป็น Webhook URL
```

---

## 🎯 Checklist

- [ ] สร้าง LINE Official Account
- [ ] ได้ Channel ID, Secret, Access Token
- [ ] เชื่อมต่อผ่าน Dashboard (Settings → Channels)
- [ ] ทดสอบการเชื่อมต่อ (ปุ่ม Test)
- [ ] บันทึก
- [ ] Deploy app (Vercel/ngrok)
- [ ] ตั้ง Webhook URL ใน LINE
- [ ] Verify webhook (Success)
- [ ] เปิด "Use webhook"
- [ ] ปิด auto-reply ของ LINE
- [ ] เพิ่ม Bot เป็นเพื่อน
- [ ] ทดสอบส่งข้อความ
- [ ] ตอบกลับจาก Dashboard

---

## 🐛 ปัญหาที่พบบ่อย

### ❌ ทดสอบไม่ผ่าน

**แก้ไข:**
- ตรวจสอบ Credentials ว่าถูกต้อง
- ตรวจสอบ Access Token ว่ายังไม่หมดอายุ
- ตรวจสอบ copy/paste ไม่มีช่องว่าง

### ❌ Webhook Verify Failed

**แก้ไข:**
- แน่ใจว่า Deploy แล้ว (ไม่ใช่ localhost)
- URL ต้องเป็น HTTPS
- Format: `https://your-domain.com/api/webhooks/line`

### ❌ ไม่เห็นข้อความใน Dashboard

**แก้ไข:**
- ตรวจสอบ Webhook เปิดอยู่
- ดู Logs ใน Vercel
- ตรวจสอบ Channel status = "connected"

---

## 🎉 เรียบร้อย!

ตอนนี้คุณมี:
- ✅ LINE Chat ทำงานผ่าน Dashboard
- ✅ Auto-reply อัตโนมัติ
- ✅ เก็บประวัติการสนทนา
- ✅ Real-time updates

---

**Next: เพิ่ม Facebook Messenger และ Instagram! 🚀**

